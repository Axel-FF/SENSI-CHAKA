-- ==============================================
-- SENSI CHAKA • @unrandom_tk
-- ==============================================

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local CoreGui = game:GetService("CoreGui")
local StarterGui = game:GetService("StarterGui")
local Workspace = game:GetService("Workspace")
local TweenService = game:GetService("TweenService")

local LP = Players.LocalPlayer
local Camera = Workspace.CurrentCamera
local MainConnection = nil

-- ==============================================
-- 🔧 TU SCREENGUI — EXACTAMENTE LA QUE FUNCIONA
-- ==============================================
local parent
pcall(function() parent = gethui() end)
if not parent then
	pcall(function() parent = CoreGui end)
end
if not parent then
	parent = LP:WaitForChild("PlayerGui")
end

pcall(function()
	for _, v in pairs(parent:GetChildren()) do
		if v.Name == "SensiChaka" or v.Name == "SensiChakaIntro" then
			v:Destroy()
		end
	end
end)

local gui = Instance.new("ScreenGui")
gui.Name = "SensiChaka"
gui.ResetOnSpawn = false
gui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
gui.DisplayOrder = 999999
gui.IgnoreGuiInset = true
gui.Parent = parent

-- ==============================================
-- 🎬 ANIMACIÓN INTRO ESTILO NINJA AIM
-- ==============================================
local function PlayIntro()
	local intro = Instance.new("ScreenGui")
	intro.Name = "SensiChakaIntro"
	intro.ResetOnSpawn = false
	intro.DisplayOrder = 9999999
	intro.IgnoreGuiInset = true
	intro.Parent = parent
	
	local bg = Instance.new("Frame")
	bg.Size = UDim2.new(1, 0, 1, 0)
	bg.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
	bg.BackgroundTransparency = 1
	bg.BorderSizePixel = 0
	bg.Parent = intro
	
	local container = Instance.new("Frame")
	container.Size = UDim2.new(0, 600, 0, 200)
	container.Position = UDim2.new(0.5, -300, 0.5, -100)
	container.BackgroundTransparency = 1
	container.Parent = intro
	
	local title = Instance.new("TextLabel")
	title.Size = UDim2.new(1, 0, 0, 90)
	title.BackgroundTransparency = 1
	title.Text = "SENSI CHAKA"
	title.Font = Enum.Font.GothamBlack
	title.TextSize = 72
	title.TextColor3 = Color3.fromRGB(255, 140, 0)
	title.TextStrokeTransparency = 0
	title.TextStrokeColor3 = Color3.fromRGB(0, 0, 0)
	title.TextTransparency = 1
	title.AnchorPoint = Vector2.new(0.5, 0.5)
	title.Position = UDim2.new(0.5, 0, 0, 45)
	title.Parent = container
	
	local titleGlow = title:Clone()
	titleGlow.TextColor3 = Color3.fromRGB(255, 200, 100)
	titleGlow.TextStrokeTransparency = 1
	titleGlow.TextTransparency = 1
	titleGlow.Position = UDim2.new(0.5, 2, 0, 47)
	titleGlow.ZIndex = title.ZIndex - 1
	titleGlow.Parent = container
	
	local sub = Instance.new("TextLabel")
	sub.Size = UDim2.new(1, 0, 0, 40)
	sub.Position = UDim2.new(0, 0, 0, 95)
	sub.BackgroundTransparency = 1
	sub.Text = "TIKTOK @unrandom_tk"
	sub.Font = Enum.Font.GothamBold
	sub.TextSize = 28
	sub.TextColor3 = Color3.fromRGB(255, 220, 180)
	sub.TextStrokeTransparency = 0.3
	sub.TextStrokeColor3 = Color3.fromRGB(0, 0, 0)
	sub.TextTransparency = 1
	sub.Parent = container
	
	local line = Instance.new("Frame")
	line.Size = UDim2.new(0, 0, 0, 3)
	line.Position = UDim2.new(0.5, 0, 0, 145)
	line.BackgroundColor3 = Color3.fromRGB(255, 140, 0)
	line.BorderSizePixel = 0
	line.Parent = container
	
	local tweenInfoIn = TweenInfo.new(0.5, Enum.EasingStyle.Quad, Enum.EasingDirection.Out)
	local tweenInfoOut = TweenInfo.new(0.6, Enum.EasingStyle.Quad, Enum.EasingDirection.In)
	local tweenInfoLine = TweenInfo.new(0.4, Enum.EasingStyle.Quad, Enum.EasingDirection.Out)
	
	task.wait(0.2)
	TweenService:Create(bg, tweenInfoIn, {BackgroundTransparency = 0.7}):Play()
	task.wait(0.15)
	
	TweenService:Create(title, tweenInfoIn, {TextTransparency = 0}):Play()
	TweenService:Create(titleGlow, tweenInfoIn, {TextTransparency = 0.3}):Play()
	title:TweenSize(UDim2.new(1.1, 0, 0, 100), Enum.EasingDirection.Out, Enum.EasingStyle.Back, 0.4, true)
	task.wait(0.1)
	title:TweenSize(UDim2.new(1, 0, 0, 90), Enum.EasingDirection.Out, Enum.EasingStyle.Quad, 0.3, true)
	
	task.wait(0.25)
	TweenService:Create(sub, tweenInfoIn, {TextTransparency = 0}):Play()
	sub:TweenPosition(UDim2.new(0, 0, 0, 95), Enum.EasingDirection.Out, Enum.EasingStyle.Back, 0.35, true)
	
	task.wait(0.2)
	TweenService:Create(line, tweenInfoLine, {Size = UDim2.new(0.6, 0, 0, 3), Position = UDim2.new(0.2, 0, 0, 145)}):Play()
	
	task.wait(2.5)
	
	TweenService:Create(title, tweenInfoOut, {TextTransparency = 1}):Play()
	TweenService:Create(titleGlow, tweenInfoOut, {TextTransparency = 1}):Play()
	TweenService:Create(sub, tweenInfoOut, {TextTransparency = 1}):Play()
	TweenService:Create(line, tweenInfoOut, {Size = UDim2.new(0, 0, 0, 3), Position = UDim2.new(0.5, 0, 0, 145)}):Play()
	TweenService:Create(bg, tweenInfoOut, {BackgroundTransparency = 1}):Play()
	
	task.wait(0.7)
	intro:Destroy()
end

task.spawn(PlayIntro)

-- ==============================================
-- ⚙️ CONFIG
-- ==============================================
pcall(function()
	StarterGui:SetCore("SendNotification", {
		Title = "SENSI CHAKA",
		Text = "TIKTOK @unrandom_tk creo este script",
		Duration = 4
	})
end)

local S = {
	ESP = false,
	Box = true,
	Name = true,
	Dist = true,
	Health = true,
	Tracer = true,
	ESPColor = Color3.fromRGB(255, 140, 0),
	Aimbot = false,
	Legit = false,
	Rage = false,
	AimKill = false,
	FOV = 120,
	ShowFOV = true,
	WallCheck = true,
	TeamCheck = true,
}

local ESPObjects = {}
local FOVCircle = nil

pcall(function()
	if Drawing then
		FOVCircle = Drawing.new("Circle")
		FOVCircle.Thickness = 1.5
		FOVCircle.NumSides = 60
		FOVCircle.Radius = S.FOV
		FOVCircle.Color = S.ESPColor
		FOVCircle.Visible = false
		FOVCircle.Filled = false
	end
end)

-- ==============================================
-- 📋 MENÚ COMPLETO (contenido dentro de tu ScreenGui)
-- ==============================================
local Main = Instance.new("Frame")
Main.Size = UDim2.new(0, 270, 0, 400)
Main.Position = UDim2.new(0.5, -135, 0.5, -200)
Main.BackgroundColor3 = Color3.fromRGB(25, 15, 8)
Main.BorderSizePixel = 0
Main.Active = true
Main.Draggable = true
Main.Parent = gui

local corner = Instance.new("UICorner")
corner.CornerRadius = UDim.new(0, 12)
corner.Parent = Main

local stroke = Instance.new("UIStroke")
stroke.Color = Color3.fromRGB(255, 140, 0)
stroke.Thickness = 2
stroke.Parent = Main

-- Header
local Header = Instance.new("Frame")
Header.Size = UDim2.new(1, 0, 0, 50)
Header.BackgroundColor3 = Color3.fromRGB(255, 120, 0)
Header.BorderSizePixel = 0
Header.ClipsDescendants = true
Header.Parent = Main

local headerCorner = Instance.new("UICorner")
headerCorner.CornerRadius = UDim.new(0, 12)
headerCorner.Parent = Header

local headerMask = Instance.new("Frame")
headerMask.Size = UDim2.new(1, 0, 0.5, 0)
headerMask.Position = UDim2.new(0, 0, 0.5, 0)
headerMask.BackgroundColor3 = Color3.fromRGB(255, 120, 0)
headerMask.BorderSizePixel = 0
headerMask.Parent = Header

local Title = Instance.new("TextLabel")
Title.Size = UDim2.new(1, -90, 0, 26)
Title.Position = UDim2.new(0, 12, 0, 4)
Title.BackgroundTransparency = 1
Title.Text = "SENSI CHAKA"
Title.Font = Enum.Font.GothamBold
Title.TextSize = 17
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.TextXAlignment = Enum.TextXAlignment.Left
Title.ZIndex = 2
Title.Parent = Header

local Credits = Instance.new("TextLabel")
Credits.Size = UDim2.new(1, -90, 0, 16)
Credits.Position = UDim2.new(0, 12, 0, 30)
Credits.BackgroundTransparency = 1
Credits.Text = "@unrandom_tk"
Credits.Font = Enum.Font.Gotham
Credits.TextSize = 12
Credits.TextColor3 = Color3.fromRGB(255, 230, 200)
Credits.TextXAlignment = Enum.TextXAlignment.Left
Credits.ZIndex = 2
Credits.Parent = Header

local MinBtn = Instance.new("TextButton")
MinBtn.Size = UDim2.new(0, 34, 0, 34)
MinBtn.Position = UDim2.new(1, -78, 0, 8)
MinBtn.BackgroundColor3 = Color3.fromRGB(200, 90, 0)
MinBtn.Text = "-"
MinBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
MinBtn.Font = Enum.Font.GothamBold
MinBtn.TextSize = 20
MinBtn.ZIndex = 2
MinBtn.Parent = Header
Instance.new("UICorner", MinBtn).CornerRadius = UDim.new(0, 8)

local CloseBtn = Instance.new("TextButton")
CloseBtn.Size = UDim2.new(0, 34, 0, 34)
CloseBtn.Position = UDim2.new(1, -40, 0, 8)
CloseBtn.BackgroundColor3 = Color3.fromRGB(180, 40, 40)
CloseBtn.Text = "X"
CloseBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
CloseBtn.Font = Enum.Font.GothamBold
CloseBtn.TextSize = 15
CloseBtn.ZIndex = 2
CloseBtn.Parent = Header
Instance.new("UICorner", CloseBtn).CornerRadius = UDim.new(0, 8)

-- Scroll
local Scroll = Instance.new("ScrollingFrame")
Scroll.Size = UDim2.new(1, -16, 1, -66)
Scroll.Position = UDim2.new(0, 8, 0, 58)
Scroll.BackgroundTransparency = 1
Scroll.BorderSizePixel = 0
Scroll.ScrollBarThickness = 4
Scroll.ScrollBarImageColor3 = Color3.fromRGB(255, 140, 0)
Scroll.CanvasSize = UDim2.new(0, 0, 0, 780)
Scroll.Parent = Main

-- Función botones
local function MakeBtn(text, y, on)
	local b = Instance.new("TextButton")
	b.Size = UDim2.new(1, -8, 0, 38)
	b.Position = UDim2.new(0, 4, 0, y)
	b.BackgroundColor3 = on and Color3.fromRGB(255, 120, 0) or Color3.fromRGB(50, 30, 15)
	b.Text = text .. (on and "  • ON" or "  • OFF")
	b.Font = Enum.Font.GothamBold
	b.TextSize = 14
	b.TextColor3 = Color3.fromRGB(255, 255, 255)
	b.Parent = Scroll
	Instance.new("UICorner", b).CornerRadius = UDim.new(0, 8)
	return b
end

local BESP    = MakeBtn("ESP Master", 8, false)
local BBox    = MakeBtn("Caja 2D", 54, true)
local BName   = MakeBtn("Nombres", 100, true)
local BDist   = MakeBtn("Distancia", 146, true)
local BHealth = MakeBtn("Vida", 192, true)
local BTracer = MakeBtn("Líneas", 238, true)

local BAim    = MakeBtn("Aimbot", 300, false)
local BLegit  = MakeBtn("Aimbot Legit", 346, false)
local BRage   = MakeBtn("Aimbot Rage", 392, false)
local BKill   = MakeBtn("AimKill", 438, false)
local BFOV    = MakeBtn("Mostrar FOV", 484, true)
local BWall   = MakeBtn("WallCheck", 530, true)
local BTeam   = MakeBtn("TeamCheck", 576, true)

local FOVLbl = Instance.new("TextLabel")
FOVLbl.Size = UDim2.new(1, -8, 0, 22)
FOVLbl.Position = UDim2.new(0, 4, 0, 640)
FOVLbl.BackgroundTransparency = 1
FOVLbl.Text = "FOV: 120"
FOVLbl.Font = Enum.Font.GothamBold
FOVLbl.TextSize = 14
FOVLbl.TextColor3 = Color3.fromRGB(255, 255, 255)
FOVLbl.Parent = Scroll

local FOVDn = Instance.new("TextButton")
FOVDn.Size = UDim2.new(0, 70, 0, 34)
FOVDn.Position = UDim2.new(0, 4, 0, 670)
FOVDn.BackgroundColor3 = Color3.fromRGB(50, 30, 15)
FOVDn.Text = "-"
FOVDn.TextColor3 = Color3.fromRGB(255, 255, 255)
FOVDn.Font = Enum.Font.GothamBold
FOVDn.TextSize = 20
FOVDn.Parent = Scroll
Instance.new("UICorner", FOVDn).CornerRadius = UDim.new(0, 8)

local FOVUp = Instance.new("TextButton")
FOVUp.Size = UDim2.new(0, 70, 0, 34)
FOVUp.Position = UDim2.new(1, -78, 0, 670)
FOVUp.BackgroundColor3 = Color3.fromRGB(50, 30, 15)
FOVUp.Text = "+"
FOVUp.TextColor3 = Color3.fromRGB(255, 255, 255)
FOVUp.Font = Enum.Font.GothamBold
FOVUp.TextSize = 20
FOVUp.Parent = Scroll
Instance.new("UICorner", FOVUp).CornerRadius = UDim.new(0, 8)

-- Toggle
local function Set(btn, state, name)
	btn.Text = name .. (state and "  • ON" or "  • OFF")
	btn.BackgroundColor3 = state and Color3.fromRGB(255, 120, 0) or Color3.fromRGB(50, 30, 15)
end

-- Conexiones botones
BESP.MouseButton1Click:Connect(function()
	S.ESP = not S.ESP
	Set(BESP, S.ESP, "ESP Master")
end)
BBox.MouseButton1Click:Connect(function()
	S.Box = not S.Box
	Set(BBox, S.Box, "Caja 2D")
end)
BName.MouseButton1Click:Connect(function()
	S.Name = not S.Name
	Set(BName, S.Name, "Nombres")
end)
BDist.MouseButton1Click:Connect(function()
	S.Dist = not S.Dist
	Set(BDist, S.Dist, "Distancia")
end)
BHealth.MouseButton1Click:Connect(function()
	S.Health = not S.Health
	Set(BHealth, S.Health, "Vida")
end)
BTracer.MouseButton1Click:Connect(function()
	S.Tracer = not S.Tracer
	Set(BTracer, S.Tracer, "Líneas")
end)

BAim.MouseButton1Click:Connect(function()
	S.Aimbot = not S.Aimbot
	Set(BAim, S.Aimbot, "Aimbot")
end)
BLegit.MouseButton1Click:Connect(function()
	S.Legit = not S.Legit
	Set(BLegit, S.Legit, "Aimbot Legit")
end)
BRage.MouseButton1Click:Connect(function()
	S.Rage = not S.Rage
	Set(BRage, S.Rage, "Aimbot Rage")
end)
BKill.MouseButton1Click:Connect(function()
	S.AimKill = not S.AimKill
	Set(BKill, S.AimKill, "AimKill")
end)
BFOV.MouseButton1Click:Connect(function()
	S.ShowFOV = not S.ShowFOV
	Set(BFOV, S.ShowFOV, "Mostrar FOV")
end)
BWall.MouseButton1Click:Connect(function()
	S.WallCheck = not S.WallCheck
	Set(BWall, S.WallCheck, "WallCheck")
end)
BTeam.MouseButton1Click:Connect(function()
	S.TeamCheck = not S.TeamCheck
	Set(BTeam, S.TeamCheck, "TeamCheck")
end)

FOVDn.MouseButton1Click:Connect(function()
	S.FOV = math.max(30, S.FOV - 10)
	FOVLbl.Text = "FOV: " .. S.FOV
end)
FOVUp.MouseButton1Click:Connect(function()
	S.FOV = math.min(400, S.FOV + 10)
	FOVLbl.Text = "FOV: " .. S.FOV
end)

-- Minimizar
local minimized = false
MinBtn.MouseButton1Click:Connect(function()
	minimized = not minimized
	if minimized then
		Main.Size = UDim2.new(0, 270, 0, 50)
		Scroll.Visible = false
		MinBtn.Text = "+"
	else
		Main.Size = UDim2.new(0, 270, 0, 400)
		Scroll.Visible = true
		MinBtn.Text = "-"
	end
end)

-- Limpieza
local function Cleanup()
	S.ESP = false
	S.Aimbot = false
	S.Rage = false
	S.Legit = false
	S.AimKill = false
	
	if MainConnection then
		MainConnection:Disconnect()
		MainConnection = nil
	end
	
	pcall(function()
		if FOVCircle then
			FOVCircle.Visible = false
			FOVCircle:Remove()
			FOVCircle = nil
		end
	end)
	
	for plr, objs in pairs(ESPObjects) do
		for _, o in pairs(objs) do
			pcall(function()
				o.Visible = false
				o:Remove()
			end)
		end
		ESPObjects[plr] = nil
	end
end

CloseBtn.MouseButton1Click:Connect(function()
	Cleanup()
	gui:Destroy()
end)

print("✅ ScreenGui SENSI CHAKA abierta")

-- ==============================================
-- 🎯 LÓGICA ESP + AIMBOT
-- ==============================================
local function IsEnemy(plr)
	if not S.TeamCheck then return true end
	if not LP.Team or not plr.Team then return true end
	return LP.Team ~= plr.Team
end

local function IsVisible(part)
	if not S.WallCheck then return true end
	local success, result = pcall(function()
		local origin = Camera.CFrame.Position
		local direction = (part.Position - origin).Unit * (part.Position - origin).Magnitude
		local raycastParams = RaycastParams.new()
		raycastParams.FilterDescendantsInstances = {LP.Character, part.Parent}
		raycastParams.FilterType = Enum.RaycastFilterType.Exclude
		raycastParams.IgnoreWater = true
		local hit = Workspace:Raycast(origin, direction, raycastParams)
		return hit == nil
	end)
	return success and result
end

local function GetClosest()
	local closest, best = nil, S.FOV
	local center = Vector2.new(Camera.ViewportSize.X/2, Camera.ViewportSize.Y/2)
	for _, plr in pairs(Players:GetPlayers()) do
		if plr ~= LP and plr.Character and IsEnemy(plr) then
			local hum = plr.Character:FindFirstChildOfClass("Humanoid")
			local head = plr.Character:FindFirstChild("Head")
			if hum and hum.Health > 0 and head then
				local pos, onScreen = Camera:WorldToViewportPoint(head.Position)
				if onScreen then
					local dist = (Vector2.new(pos.X, pos.Y) - center).Magnitude
					if dist < best and IsVisible(head) then
						best = dist
						closest = head
					end
				end
			end
		end
	end
	return closest
end

local function ClearESP(plr)
	if ESPObjects[plr] then
		for _, o in pairs(ESPObjects[plr]) do
			pcall(function()
				o.Visible = false
				o:Remove()
			end)
		end
		ESPObjects[plr] = nil
	end
end

local function CreateESP(plr)
	if ESPObjects[plr] then return end
	local success, objs = pcall(function()
		if not Drawing then return nil end
		local t = {}
		t.box = Drawing.new("Square")
		t.box.Thickness = 1.5
		t.box.Filled = false
		t.box.Visible = false
		t.tracer = Drawing.new("Line")
		t.tracer.Thickness = 1.2
		t.tracer.Visible = false
		t.name = Drawing.new("Text")
		t.name.Size = 14
		t.name.Center = true
		t.name.Outline = true
		t.name.Visible = false
		t.dist = Drawing.new("Text")
		t.dist.Size = 13
		t.dist.Center = true
		t.dist.Outline = true
		t.dist.Visible = false
		t.hp = Drawing.new("Line")
		t.hp.Thickness = 3
		t.hp.Visible = false
		return t
	end)
	if success and objs then
		ESPObjects[plr] = objs
	end
end

-- Loop principal
MainConnection = RunService.RenderStepped:Connect(function()
	Camera = Workspace.CurrentCamera
	
	pcall(function()
		if FOVCircle then
			FOVCircle.Position = Vector2.new(Camera.ViewportSize.X/2, Camera.ViewportSize.Y/2)
			FOVCircle.Radius = S.FOV
			FOVCircle.Color = S.ESPColor
			FOVCircle.Visible = S.ShowFOV and (S.Aimbot or S.Legit or S.Rage or S.AimKill)
		end
	end)

	if S.Aimbot or S.Legit or S.Rage or S.AimKill then
		local target = GetClosest()
		if target then
			if S.Rage or S.AimKill then
				Camera.CFrame = CFrame.new(Camera.CFrame.Position, target.Position)
			else
				local goal = CFrame.new(Camera.CFrame.Position, target.Position)
				local speed = S.Legit and 0.12 or 0.22
				Camera.CFrame = Camera.CFrame:Lerp(goal, speed)
			end
		end
	end

	if not S.ESP then
		for plr in pairs(ESPObjects) do
			ClearESP(plr)
		end
		return
	end

	for _, plr in pairs(Players:GetPlayers()) do
		if plr ~= LP and plr.Character and IsEnemy(plr) then
			local hum = plr.Character:FindFirstChildOfClass("Humanoid")
			local root = plr.Character:FindFirstChild("HumanoidRootPart")
			local head = plr.Character:FindFirstChild("Head")
			if hum and hum.Health > 0 and root and head then
				CreateESP(plr)
				local o = ESPObjects[plr]
				if o then
					local pos, onScreen = Camera:WorldToViewportPoint(root.Position)
					local headPos = Camera:WorldToViewportPoint(head.Position + Vector3.new(0, 0.5, 0))
					local footPos = Camera:WorldToViewportPoint(root.Position - Vector3.new(0, 3, 0))
					if onScreen then
						local h = math.abs(headPos.Y - footPos.Y)
						local w = h * 0.55
						local boxPos = Vector2.new(pos.X - w/2, pos.Y - h/2)
						o.box.Size = Vector2.new(w, h)
						o.box.Position = boxPos
						o.box.Color = S.ESPColor
						o.box.Visible = S.Box
						o.tracer.From = Vector2.new(Camera.ViewportSize.X/2, Camera.ViewportSize.Y)
						o.tracer.To = Vector2.new(pos.X, pos.Y)
						o.tracer.Color = S.ESPColor
						o.tracer.Visible = S.Tracer
						o.name.Text = plr.Name
						o.name.Position = Vector2.new(pos.X, headPos.Y - 16)
						o.name.Color = S.ESPColor
						o.name.Visible = S.Name
						local d = math.floor((root.Position - Camera.CFrame.Position).Magnitude)
						o.dist.Text = d .. "m"
						o.dist.Position = Vector2.new(pos.X, footPos.Y + 2)
						o.dist.Color = S.ESPColor
						o.dist.Visible = S.Dist
						local pct = math.clamp(hum.Health / math.max(hum.MaxHealth, 1), 0, 1)
						o.hp.From = Vector2.new(boxPos.X - 5, boxPos.Y + h)
						o.hp.To = Vector2.new(boxPos.X - 5, boxPos.Y + h * (1 - pct))
						o.hp.Color = Color3.fromRGB(255 * (1 - pct), 255 * pct, 40)
						o.hp.Visible = S.Health
					else
						o.box.Visible = false
						o.tracer.Visible = false
						o.name.Visible = false
						o.dist.Visible = false
						o.hp.Visible = false
					end
				end
			else
				ClearESP(plr)
			end
		else
			ClearESP(plr)
		end
	end
end)

Players.PlayerRemoving:Connect(ClearESP)
print("✅ SENSI CHAKA listo")
